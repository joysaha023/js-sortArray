// const colors = ['red', 'blue', 'green', 'yellow', 'orange'];

// function reverseColors(array) {
//     const length = array.length;
//     for(let i = 0; i < Math.floor(length / 2); i++){
//         const temp = array[i];
//         array[i] = array[length - 1 - i];
//         array[length - 1 - i] = temp;
//     }
// }

// reverseColors(colors);

// console.log(colors);


